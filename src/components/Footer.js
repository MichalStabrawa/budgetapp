import React from "react";

const Footer = (props) => {
  return (
    <footer>
      <p>
        Footer {props.name} <i className="fas fa-thumbs-up fa-5x"></i>
      </p>
    </footer>
  );
};

export default Footer;
